{
  "name": "lambda-client-sdk",
  "version": "0.0.5",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Client SDK for Lambda Protocol. Querying single or multiple data points from a smart contract in the Lambda Protocol.",
  "scripts": {
    "build": "bun run build.mjs"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "bun"
  ],
  "license": "GNU GPLv3",
  "devDependencies": {
    "bun-plugin-dts": "^0.2.1",
    "@types/bun": "^1.0.0",
    "typescript": "^5.2.2",
    "prettier": "^3.1.1",
    "@types/json-bigint": "^1.0.4"
  },
  "dependencies": {
    "json-bigint": "^1.0.0"
  }
}
